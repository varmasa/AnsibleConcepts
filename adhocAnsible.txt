syntax: ansible <hosts> -m <module> -a "<arguments>"

ansible dev -m ping
ansible dev -m command -a "uptime"
ansible dev -m shell -a "df -h"
ansible dev -b -m reboot
ansible dev -b -m yum -a "name=httpd state=present"
ansible dev -b -m service -a "name=httpd tate=started"
