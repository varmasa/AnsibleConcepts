Forks are used to control parllalesm in hosts it runs sequentially based on fork number

ansible-playbook variables.yaml -f 1  //Here we have 2 servers then ansible runs one after another sequentially

ansible-playbook variables.yaml -f 2  //Here we have 4 severs then ansible runs two servers sequentially then another two servers
